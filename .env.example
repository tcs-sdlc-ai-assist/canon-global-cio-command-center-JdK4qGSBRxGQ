# Canon CIO Command Center - Environment Variables
# This static frontend MVP requires no environment variables.
# All configuration is handled via constants in the source code.
# If you need to add environment variables in the future, define them here
# with the VITE_ prefix (e.g., VITE_API_URL=https://api.example.com).